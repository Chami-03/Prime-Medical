# PrimeMedical Frontend

This is the React + TypeScript frontend for PrimeMedical built with Vite and Tailwind.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Environment:
- `VITE_API_BASE` — base URL for backend API (defaults to `http://localhost:8080`)
